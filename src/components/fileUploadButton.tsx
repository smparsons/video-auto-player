import { Button } from '@material-ui/core'
import * as React from 'react'

export const FileUploadButton = (): JSX.Element => {
    return (
        <>
            <Button variant="contained" color="primary">
                Import Playlist
            </Button>
        </>
    );
};
